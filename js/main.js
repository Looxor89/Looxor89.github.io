//console.log("1!!");

jQuery(document).ready(function() {
    //console.log("2 ready")
    var lista = jQuery("#list");
    //console.log(lista);

    /*var homeMenu = jQuery("#mainMenu .active a").attr("href")

    console.log(homeMenu)

    jQuery("#bottone").on("click", createBox)

    var theClass = getColor();

    function createBox() {
        var $newBox = jQuery("<div class='box'>Sono il Box che cercavi :D</div>")

        console.log("il nuovo colore è " + theClass);

        $newBox.addClass(theClass).text("Box2").appendTo(".titolo")
            //$newBox.clone().text("box 3").appendTo(".container")
    }

    function getColor() {
        var num = Math.round(Math.random())
        console.log("num", num)
        if (num === o) { return "blue" } else { return "orange" }
    };
    createBox();*/
})